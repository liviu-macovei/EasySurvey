@model EasySurvey.Web.ViewModels.AnswerSections.AnswerSectionsViewModel
<table class="table">   
    @{
        @for (int i = 0; i < Model.Count(); i++)
        {
            <tr class="row">
                @Html.EditorFor(m => Model[i])
            </tr>
        }
    }
</table>